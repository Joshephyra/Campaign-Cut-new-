# AGENTS.md

The project rules live in `CLAUDE.md`. Read that file at the start of every session; it applies to every agent working in this repository, whatever tool runs it.

Two things it says in Claude Code terms that read the same for any other agent: "Claude Code prompt" means the agent's own prompt, and every session ends with a reminder to push to GitHub.
