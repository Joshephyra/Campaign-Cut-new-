{
  "name": "@campaigncut/ingest",
  "private": true,
  "version": "0.0.1",
  "description": "The After Effects (Bodymovin) to template CLI.",
  "type": "module",
  "scripts": {
    "schema": "tsx src/cli-schema.ts",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "ingest": "tsx src/cli-ingest.ts"
  },
  "dependencies": {
    "@campaigncut/composition": "0.0.1",
    "@campaigncut/server": "0.0.1"
  },
  "devDependencies": {
    "@types/node": "24.13.4",
    "tsx": "4.23.13",
    "vitest": "5.0.0"
  }
}
